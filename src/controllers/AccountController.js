
const AccountController = () => {

  const createAccount = (e) => {
    e.preventDefault()
  }

  const loginAccount = (e) => {
    e.preventDefault()
  }

  return {
    createAccount,
    loginAccount
  }
}

export default AccountController